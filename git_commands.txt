git init  (to initialize empty git repository)
git status
git add <file name> or git add .
from statging to move back to untracked command is = git rm --cached <file name>
staging to tracked =  git commit -m "added tetsing files"
lets say you deleted <testing.py> command (rm testing.py) inorder to restore that file command is = git restore testing.py